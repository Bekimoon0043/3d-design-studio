# Local testing mode uses browser localStorage — no env vars required.
# Leave these empty or omit the .env file entirely.
#
# VITE_SUPABASE_URL=
# VITE_SUPABASE_ANON_KEY=
# VITE_API_BASE_URL=http://localhost:4000
