/**
 * Supabase is disabled for local testing.
 * Projects are stored in browser localStorage via useProjectPersistence.
 * Keep this file so imports don't break if you re-enable cloud later.
 */
export const isSupabaseConfigured = false;
export const supabase = null;
